# Port to bind on. Railway injects PORT automatically; defaults to 8080 locally.
PORT=8080

# Optional comma-separated allowlist of browser origins permitted to connect.
# Leave empty to allow all origins. In production set it to your Vercel domain(s):
# ALLOWED_ORIGINS=https://your-app.vercel.app
ALLOWED_ORIGINS=
